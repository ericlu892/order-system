{
  "name": "order-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd server && npm install && node server_new.js",
    "postinstall": "cd server && npm install"
  },
  "dependencies": {}
}
